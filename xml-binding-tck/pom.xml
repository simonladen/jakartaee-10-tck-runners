<?xml version="1.0" encoding="UTF-8"?>
<!--
  DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS HEADER.

  Copyright (c) 2024 Payara Foundation and/or its affiliates. All rights reserved.

  The contents of this file are subject to the terms of either the GNU
  General Public License Version 2 only ("GPL") or the Common Development
  and Distribution License("CDDL") (collectively, the "License").  You
  may not use this file except in compliance with the License.  You can
  obtain a copy of the License at
  https://github.com/payara/Payara/blob/master/LICENSE.txt
  See the License for the specific
  language governing permissions and limitations under the License.

  When distributing the software, include this License Header Notice in each
  file and include the License file at glassfish/legal/LICENSE.txt.

  GPL Classpath Exception:
  The Payara Foundation designates this particular file as subject to the "Classpath"
  exception as provided by the Payara Foundation in the GPL Version 2 section of the License
  file that accompanied this code.

  Modifications:
  If applicable, add the following below the License Header, with the fields
  enclosed by brackets [] replaced by your own identifying information:
  "Portions Copyright [year] [name of copyright owner]"

  Contributor(s):
  If you wish your version of this file to be governed by only the CDDL or
  only the GPL Version 2, indicate your decision by adding "[Contributor]
  elects to include this software in this distribution under the [CDDL or GPL
  Version 2] license."  If you don't indicate a single choice of license, a
  recipient has the option to distribute your version of this file under
  either the CDDL, the GPL Version 2 or to extend the choice of license to
  its licensees as provided above.  However, if you add GPL Version 2 code
  and therefore, elected the GPL Version 2 license, then the option applies
  only if the new code is made subject to such option by the copyright
  holder.

-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>fish.payara.jakarta.tests.tck</groupId>
        <artifactId>tck</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>xml-binding-tck</artifactId>
    <packaging>pom</packaging>

    <name>TCK: Activation</name>

    <properties>
        <ant.home>${project.build.directory}${file.separator}apache-ant-${ant.version}</ant.home>
        <ant.zip.url>https://archive.apache.org/dist/ant/binaries/apache-ant-${ant.version}-bin.zip</ant.zip.url>
        <ant.executable>${ant.home}${file.separator}bin${file.separator}ant</ant.executable>
        <tck.home>${project.build.directory}${file.separator}xml-binding-tck</tck.home>
        <os.folder>linux</os.folder>
        <os.extension>sh</os.extension>
        <os.script.command>"/bin/sh"</os.script.command>
    </properties>

    <dependencies>
        <dependency>
            <groupId>fish.payara.jakarta.tests.tck</groupId>
            <artifactId>jakarta-xml-binding-tck</artifactId>
            <version>${project.version}</version>
            <type>zip</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Download Ant -->
            <plugin>
                <groupId>com.googlecode.maven-download-plugin</groupId>
                <artifactId>download-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>download-ant</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>wget</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skip>${skipTests}</skip>
                    <url>${ant.zip.url}</url>
                    <unpack>true</unpack>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                </configuration>
            </plugin>

            <!-- Unpack Payara and TCK -->
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <skip>${skipTests}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>unpack-tck</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeArtifactIds>jakarta-xml-binding-tck</includeArtifactIds>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Configure and run TCK -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>org.apache.ant</groupId>
                        <artifactId>ant</artifactId>
                        <version>${ant.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>1.0b3</version>
                        <exclusions>
                            <exclusion>
                                <groupId>ant</groupId>
                                <artifactId>ant</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                </dependencies>
                <configuration>
                    <skip>${skipTests}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-tck-and-payara</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target xmlns:if="ant:if" xmlns:unless="ant:unless">
                                <taskdef resource="net/sf/antcontrib/antcontrib.properties" classpathref="maven.plugin.classpath" />

                                <macrodef name="tck-setting">
                                    <attribute name="key" /> <attribute name="value" />
                                    <sequential>
                                        <replaceregexp file="${tck.home}/lib/javasoft-multiJVM.jti" byline="true"
                                                       match="@{key}=.*" replace="@{key}=@{value}" />
                                        <replaceregexp file="${tck.home}/lib/javasoft-singleJVM.jti" byline="true"
                                                       match="@{key}=.*" replace="@{key}=@{value}" />
                                    </sequential>
                                </macrodef>
                                
                                <macrodef name="testsuite-setting">
                                    <attribute name="key" /> <attribute name="value" />
                                    <sequential>
                                        <replaceregexp file="${tck.home}/testsuite.jtt" byline="true"
                                                       match="@{key}=.*" replace="@{key}=@{value}" />
                                    </sequential>
                                </macrodef>


                                <!-- Change configuration -->
                                <testsuite-setting key="finder" value="com.sun.javatest.finder.BinaryTestFinder -binary ${tck.home}/tests/testsuite.jtd"/>
                                
                                <tck-setting key="TESTSUITE" value="${tck.home}"/>
                                <tck-setting key="WORKDIR" value="${tck.home}/batch-multiJVM/work"/>
                                <tck-setting key="jck.env.jaxb.classes.jaxbClasses" value="${tck.home}/glassfish/modules/jakarta.xml.bind-api.jar ${tck.home}/glassfish/modules/jakarta.activation-api.jar ${tck.home}/glassfish/modules/jaxb-core.jar ${tck.home}/glassfish/modules/jaxb-impl.jar ${tck.home}/glassfish/modules/jaxb-jxc.jar checker.jar"/>
                                <tck-setting key="jck.env.jaxb.testExecute.cmdAsFile" value="${java.home}/bin/java"/>
                                <tck-setting key="jck.env.jaxb.testExecute.otherEnvVars" value="JAVA_HOME\=${java.home} JAXB_HOME\=${payara.home}/glassfish/modules"/>
                                <tck-setting key="jck.env.jaxb.schemagen.run.jxcCmd" value="${os.script.command} ${tck.home}/${os.folder}/bin/schemagen.${os.extension}"/>
                                <tck-setting key="jck.env.jaxb.xsd_compiler.testCompile.xjcCmd" value="${os.script.command} ${tck.home}/${os.folder}/bin/xjc.${os.extension}"/>
                                

                                <!-- <replace file="${tck.home}/build.xml">
                                    <replacetoken><![CDATA[<sysproperty key="testDebug" value="${tests.debug}"/>]]></replacetoken>
                                    <replacevalue><![CDATA[<sysproperty key="testDebug" value="${tests.debug}"/>
                                <jvmarg value="-Djavatest.security.noSecurityManager=true"/>]]></replacevalue>
                                </replace> -->
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>run-tck-tests</id>
                        <phase>integration-test</phase>
                        <goals>
                                <goal>run</goal>
                        </goals>
                        <configuration>
                                <systemPropertyVariables>
                                        <TS_HOME>${tck.home}</TS_HOME>
                                        <JAXB_HOME>${payara.home}/glassfish/modules</JAXB_HOME>
                                        <JAVA_HOME>${java.home}</JAVA_HOME>
                                </systemPropertyVariables>
                                <target xmlns:if="ant:if" xmlns:unless="ant:unless">
                                        <taskdef resource="net/sf/antcontrib/antcontrib.properties" classpathref="maven.plugin.classpath" />

                                        <echo level="info" message="Start running all tests" />
                                        <exec executable="${ant.executable}" dir="${tck.home}" resultproperty="testResult">
                                            <arg value="runclient" />
                                            <arg value="-DfailOnError=false" />
                                            <env key="JAVA_HOME" value="${java.home}"/>
                                            <env key="JAXB_HOME" value="${payara.home}${file.separator}glassfish${file.separator}modules" />
                                            <env key="TS_HOME" value="${tck.home}" />
                                        </exec>

                                        <if>
                                            <not>
                                                <equals arg1="${testResult}" arg2="0" />
                                            </not>
                                            <then>
                                                <echo message="Running tests failed." />
                                                <loadfile property="contents" srcFile="${payara.home}${file.separator}glassfish${file.separator}domains${file.separator}domain1${file.separator}logs${file.separator}server.log" />
                                                <echo message="${contents}" />
                                            </then>
                                        </if>

                                        <if>
                                            <not>
                                                <equals arg1="${testResult}" arg2="0" />
                                            </not>
                                            <then>
                                                <fail/>
                                            </then>
                                        </if>
                                </target>
                            </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--<plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>integration-test</phase>
                        <goals>
                                <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${java.home}/bin/java</executable>
                            <arguments>
                                <argument>-jar ${tck.home}\lib\javatest.jar</argument>
                                <argument>-batch</argument>
                                <argument>-testsuite ${tck.home}</argument>
                                <argument>-open ${tck.home}\lib\javasoft-multiJVM.jti</argument>
                                <argument>-workdir -create ${tck.home}\work</argument>
                                <argument>-runtests</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin> -->
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
            </activation>
            <properties>
                <os.folder>win32</os.folder>
                <os.extension>bat</os.extension>
                <os.script.command></os.script.command>
                <ant.executable>${ant.home}${file.separator}bin${file.separator}ant.cmd</ant.executable>
            </properties>
        </profile>
    </profiles>
</project>